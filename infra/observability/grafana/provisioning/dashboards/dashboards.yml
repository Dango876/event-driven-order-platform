apiVersion: 1

providers:
  - name: EDOP Local
    orgId: 1
    folder: EDOP Local
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
